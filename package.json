{
  "name": "pdc-integrations",
  "version": "0.2.0",
  "description": "Pipedream Connect CLI — your personal integration toolkit",
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts --",
    "build": "bun build src/cli.ts --compile --outfile pdc",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@pipedream/sdk": "^1.0.1",
    "commander": "^12.1.0",
    "chalk": "^5.4.1",
    "cli-table3": "^0.6.5",
    "@inquirer/prompts": "^7.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
